import type { SectionId } from "../types/portfolio";

export const sectionIds: SectionId[] = ["home", "about", "projects", "skills", "contact"];
export const desktopMediaQuery = "(min-width: 1000px) and (min-height: 720px)";
